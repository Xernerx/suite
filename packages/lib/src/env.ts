/** @format */

export const ENV = {
	// Server-side only
	DOMAIN: process.env.DOMAIN || 'xernerx.com',
	ENVIRONMENT: process.env.ENVIRONMENT || 'DEVELOPMENT',
	NEXT_PUBLIC_BASE_URL: process.env.NEXT_PUBLIC_BASE_URL || 'xernerx.com',
};
